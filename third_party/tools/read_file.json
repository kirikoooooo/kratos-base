{
  "type": "function",
  "function": {
    "name": "read_file",
    "description": "读取仓库内文件或列出目录。传文件路径返回内容；传目录路径（如 internal/biz）返回条目列表。分析 biz 等领域时先 read_file 目录再读各 .go 文件。",
    "parameters": {
      "type": "object",
      "properties": {
        "input": {
          "type": "string",
          "description": "读取参数。可直接传相对路径，如 README.md；也可传 JSON 字符串，如 {\"path\":\"internal/data/runtime.go\",\"start\":1,\"end\":80}。"
        }
      },
      "required": [
        "input"
      ],
      "additionalProperties": false
    },
    "strict": true
  }
}
